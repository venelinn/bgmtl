# Shared secret between Contentful's publish webhook and POST /api/revalidate.
# Use a long random string (e.g. `openssl rand -hex 24`). Set the SAME value in
# your host (Netlify → Site settings → Environment variables) and in the
# Contentful webhook header. Without it the route fails closed (500/401).
CONTENTFUL_REVALIDATE_SECRET=
