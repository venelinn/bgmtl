
[build]
  # Use build:ci (no `rm -rf .next`) so Netlify's restored `.next/cache`
  # (SWC/webpack compile cache + Next data cache) survives into the build.
  # The local `pnpm build` still cleans; CI relies on a fresh container per run.
  command = "pnpm build:ci"
